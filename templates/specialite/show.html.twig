{% extends 'base_admin.html.twig' %}

{% block title %}Specialite{% endblock %}

{% block body %}
    <h1>Specialite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialite.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ specialite.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('specialite_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>

    <a href="{{ path('specialite_edit', {'id': specialite.id}) }}"><i class="fa fa-pencil-square" aria-hidden="true"></i></a>

    {{ include('specialite/_delete_form.html.twig') }}
{% endblock %}

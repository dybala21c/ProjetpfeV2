{% extends 'base_admin.html.twig' %}

{% block title %}Grade1{% endblock %}

{% block body %}
    <h1>Grade1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade1.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ grade1.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grade1_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>

    <a href="{{ path('grade1_edit', {'id': grade1.id}) }}"><i class="fa fa-pencil-square" aria-hidden="true"></i></a>

    {{ include('grade1/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base_admin.html.twig' %}

{% block title %}Grade1{% endblock %}

{% block body %}
    <h1>Grade1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade1.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ grade1.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grade1_index') }}">Retour à la liste</a>

    <a href="{{ path('grade1_edit', {'id': grade1.id}) }}">Modifier</a>

    {{ include('grade1/_delete_form.html.twig') }}
{% endblock %}

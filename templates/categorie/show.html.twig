{% extends 'base_admin.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}"><i class="fa fa-pencil-square" aria-hidden="true"></i></a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}

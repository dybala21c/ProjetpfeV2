{% extends 'base_admin.html.twig' %}

{% block title %}Bureau{% endblock %}

{% block body %}
    <h1>Bureau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bureau.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ bureau.Numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bureau_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>

    <a href="{{ path('bureau_edit', {'id': bureau.id}) }}"><i class="fa fa-pencil-square" aria-hidden="true"></i></a>

    {{ include('bureau/_delete_form.html.twig') }}
{% endblock %}

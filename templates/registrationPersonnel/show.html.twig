{% extends 'base_admin.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Personnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnel.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personnel.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list_personnel') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>
{#
    <a href="{{ path('grade1_edit', {'id': grade1.id}) }}"><i class="fa fa-pencil-square" aria-hidden="true"></i></a>
#}
    {{ include('registrationPersonnel/_delete_form.html.twig') }}

    
{% endblock %}

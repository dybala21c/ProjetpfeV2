{% extends 'base_admin.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ annonce.Titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ annonce.Contenu }}</td>
            </tr>
            <tr>
                <th>DateAjout</th>
                <td>{{ annonce.DateAjout ? annonce.DateAjout|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('annonce_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>
     <td>
    <a href="{{ path('annonce_edit', {'id': annonce.id}) }}"><i class="fa fa-pencil-square" aria-hidden="true"></i></a>
     </td>

    {{ include('annonce/_delete_form.html.twig') }}
{% endblock %}
